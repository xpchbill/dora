import React, { useEffect } from 'react';
import { Communication } from '../components';
import { RouterProps } from '../../../types';

export const CommunicationContainer = (props: RouterProps) => {
  return (
    <Communication />
  );
};

export default CommunicationContainer;
